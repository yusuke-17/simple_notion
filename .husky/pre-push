#!/usr/bin/env sh

# nvmを初期化
if [ -s "$HOME/.nvm/nvm.sh" ]; then
    . "$HOME/.nvm/nvm.sh"
fi

# PATHを明示的に設定(nodeコマンドを使えるようにする)
export PATH="$HOME/.nvm/versions/node/v25.2.1/bin:$HOME/.nodenv/shims:$HOME/.volta/bin:/usr/local/bin:/usr/bin:/bin:$PATH"

echo "🚀 Running pre-push CI/CD checks..."

# 既存のpre-pushフックを実行
if [ -x ".git/hooks/pre-push" ]; then
    .git/hooks/pre-push "$@"
fi