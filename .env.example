# Simple Notion 環境変数設定例
# このファイルをコピーして .env ファイルを作成し、適切な値を設定してください

# === データベース設定 ===
# PostgreSQLパスワード（本番環境では必ず変更）
POSTGRES_PASSWORD=password

# データベース接続URL
# ローカル開発: postgres://postgres:password@localhost:5432/notion_app?sslmode=disable
# Docker環境: postgres://postgres:${POSTGRES_PASSWORD}@db:5432/notion_app?sslmode=disable
DATABASE_URL=postgres://postgres:password@localhost:5432/notion_app?sslmode=disable

# === 認証設定 ===
# JWT秘密鍵（セキュリティ上極めて重要）
# 以下のコマンドでランダムな強力なJWT秘密鍵を生成してください：
# openssl rand -base64 64
# または make generate-jwt-secret を実行してください
# 本番環境では必ず独自の安全な値に変更してください（最低32文字推奨）
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_KEY_IN_PRODUCTION

# === サーバー設定 ===
# アプリケーションポート
PORT=8080

# === 環境設定 ===
# 環境モード（development / production）
GO_ENV=development
NODE_ENV=development
GIN_MODE=debug

# === フロントエンド設定 ===
# APIベースURL（本番環境では空文字または相対パス）
VITE_API_BASE_URL=http://localhost:8080

# === セキュリティ設定 ===
# 環境設定 (development / production)
ENVIRONMENT=development

# Cookie セキュリティ設定
# 開発環境設定
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
COOKIE_DOMAIN=

# 本番環境では以下の設定を推奨:
# ENVIRONMENT=production
# COOKIE_SECURE=true
# COOKIE_SAMESITE=strict
# COOKIE_DOMAIN=yourdomain.com

# === 重要なセキュリティ注意事項 ===
# 1. JWT_SECRETは最低32文字の強力なランダム文字列を使用してください
# 2. 本番環境では必ず独自の安全な値に変更してください
# 3. .envファイルは絶対にGitにコミットしないでください（.gitignoreに含まれています）
# 4. データベースパスワードも本番環境では強力なものを設定してください
# 5. openssl rand -base64 64 または make generate-jwt-secret でJWT秘密鍵を生成できます
